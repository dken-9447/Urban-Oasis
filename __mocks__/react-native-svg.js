import React from "react";
import { View } from "react-native";

const Svg = (props) => <View {...props} />;
const Path = () => <View />;
const G = () => <View />;

export { Svg, Path, G };
export default Svg;
